#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include "../header/people.h"
#include <cassert>
#include "../header/help.h"

using namespace std;
#define DEBUG 1


int main () {

    if (DEBUG) {
        writeLog("699");
    } 

    // string message;
    // while(cin >> message) {
    //     cout << message << endl;
    // };

    sleep();
    return 0;
}